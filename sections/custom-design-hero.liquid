{% comment %}
  Custom Design Hero Section
  A stunning, modern, premium hero banner for the custom t-shirt studio
{% endcomment %}

<div class="custom-design-hero-section">
  <style>
    /* Premium Modern Hero Section */
    .custom-design-hero-section {
      background: linear-gradient(135deg, 
        #667eea 0%, 
        #764ba2 25%, 
        #f093fb 50%, 
        #4facfe 75%, 
        #00f2fe 100%);
      background-size: 400% 400%;
      animation: gradientShift 15s ease infinite;
      padding: 80px 20px;
      position: relative;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 600px;
    }
    
    /* Animated gradient background */
    @keyframes gradientShift {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }
    
    /* Floating blob animations */
    .hero-blob {
      position: absolute;
      border-radius: 50%;
      filter: blur(60px);
      opacity: 0.3;
      animation: float 20s ease-in-out infinite;
    }
    
    .hero-blob-1 {
      width: 400px;
      height: 400px;
      background: linear-gradient(135deg, #f093fb, #f5576c);
      top: -10%;
      left: -5%;
      animation-delay: 0s;
    }
    
    .hero-blob-2 {
      width: 350px;
      height: 350px;
      background: linear-gradient(135deg, #4facfe, #00f2fe);
      bottom: -10%;
      right: -5%;
      animation-delay: 7s;
    }
    
    .hero-blob-3 {
      width: 300px;
      height: 300px;
      background: linear-gradient(135deg, #43e97b, #38f9d7);
      top: 50%;
      right: 10%;
      animation-delay: 14s;
    }
    
    @keyframes float {
      0%, 100% {
        transform: translate(0, 0) rotate(0deg) scale(1);
      }
      33% {
        transform: translate(30px, -30px) rotate(120deg) scale(1.1);
      }
      66% {
        transform: translate(-20px, 20px) rotate(240deg) scale(0.9);
      }
    }
    
    /* Glassmorphism card */
    .hero-content {
      max-width: 950px;
      width: 100%;
      z-index: 10;
      position: relative;
      background: rgba(255, 255, 255, 0.25);
      backdrop-filter: blur(30px) saturate(180%);
      -webkit-backdrop-filter: blur(30px) saturate(180%);
      border-radius: 40px;
      padding: 70px 60px;
      border: 2px solid rgba(255, 255, 255, 0.4);
      box-shadow: 
        0 30px 80px rgba(0, 0, 0, 0.15),
        0 0 0 1px rgba(255, 255, 255, 0.6) inset,
        0 0 60px rgba(255, 255, 255, 0.2) inset;
      animation: fadeInUp 1s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(40px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    /* Typography - Modern & Bold */
    .hero-title {
      font-size: 4.5rem;
      font-weight: 900;
      color: #ffffff;
      margin: 0 0 24px 0;
      line-height: 1.1;
      letter-spacing: -0.02em;
      text-shadow: 
        0 4px 20px rgba(0, 0, 0, 0.2),
        0 0 40px rgba(255, 255, 255, 0.3);
      background: linear-gradient(135deg, 
        #ffffff 0%, 
        #f0f9ff 30%, 
        #e0f2fe 60%, 
        #ffffff 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      background-size: 300% auto;
      animation: shimmerText 6s linear infinite;
    }
    
    @keyframes shimmerText {
      0% { background-position: 0% center; }
      100% { background-position: 300% center; }
    }
    
    .hero-subtitle {
      font-size: 1.5rem;
      color: rgba(255, 255, 255, 0.95);
      margin: 0 0 20px 0;
      font-weight: 600;
      letter-spacing: 0.02em;
      text-shadow: 0 2px 10px rgba(0, 0, 0, 0.15);
    }
    
    .hero-description {
      font-size: 1.15rem;
      color: rgba(255, 255, 255, 0.9);
      margin: 0 0 40px 0;
      line-height: 1.8;
      max-width: 750px;
      margin-left: auto;
      margin-right: auto;
      text-shadow: 0 1px 5px rgba(0, 0, 0, 0.1);
      font-weight: 400;
    }
    
    /* Premium Feature Pills with Icons */
    .hero-features {
      display: flex;
      justify-content: center;
      gap: 16px;
      flex-wrap: wrap;
      margin: 40px 0 50px 0;
    }
    
    .feature-pill {
      background: rgba(255, 255, 255, 0.3);
      backdrop-filter: blur(15px);
      padding: 14px 28px;
      border-radius: 30px;
      font-size: 0.95rem;
      color: #ffffff;
      font-weight: 600;
      border: 2px solid rgba(255, 255, 255, 0.4);
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      display: inline-flex;
      align-items: center;
      gap: 10px;
      cursor: default;
      position: relative;
      overflow: hidden;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    }
    
    .feature-pill::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, 
        transparent, 
        rgba(255, 255, 255, 0.3), 
        transparent);
      transition: left 0.6s ease;
    }
    
    .feature-pill:hover::before {
      left: 100%;
    }
    
    .feature-pill:hover {
      background: rgba(255, 255, 255, 0.45);
      transform: translateY(-4px) scale(1.05);
      box-shadow: 
        0 8px 25px rgba(0, 0, 0, 0.15),
        0 0 30px rgba(255, 255, 255, 0.3);
      border-color: rgba(255, 255, 255, 0.6);
    }
    
    .feature-pill .icon {
      font-size: 1.2rem;
      animation: iconPulse 2s ease-in-out infinite;
    }
    
    @keyframes iconPulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.1); }
    }
    
    /* Premium 3D Glowing CTA Button */
    .hero-cta-button {
      background: linear-gradient(135deg, 
        #06b6d4 0%, 
        #3b82f6 25%, 
        #8b5cf6 50%, 
        #ec4899 75%, 
        #f43f5e 100%);
      background-size: 300% 300%;
      animation: gradientFlow 4s ease infinite;
      color: #ffffff;
      border: none;
      padding: 22px 60px;
      border-radius: 50px;
      font-size: 1.2rem;
      font-weight: 800;
      cursor: pointer;
      position: relative;
      overflow: hidden;
      display: inline-flex;
      align-items: center;
      gap: 12px;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      box-shadow: 
        0 12px 40px rgba(139, 92, 246, 0.4),
        0 0 0 3px rgba(255, 255, 255, 0.3),
        0 0 0 6px rgba(139, 92, 246, 0.2);
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      transform: perspective(1000px) translateZ(0);
    }
    
    @keyframes gradientFlow {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }
    
    .hero-cta-button::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.3);
      transform: translate(-50%, -50%);
      transition: width 0.6s ease, height 0.6s ease;
    }
    
    .hero-cta-button:hover::before {
      width: 400px;
      height: 400px;
    }
    
    .hero-cta-button:hover {
      transform: perspective(1000px) translateZ(20px) translateY(-6px) scale(1.05);
      box-shadow: 
        0 20px 60px rgba(139, 92, 246, 0.6),
        0 0 0 4px rgba(255, 255, 255, 0.5),
        0 0 0 8px rgba(139, 92, 246, 0.3),
        0 0 80px rgba(236, 72, 153, 0.4);
    }
    
    .hero-cta-button:active {
      transform: perspective(1000px) translateZ(10px) translateY(-2px) scale(1.02);
    }
    
    .hero-cta-button .arrow {
      font-size: 1.3rem;
      transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      z-index: 1;
    }
    
    .hero-cta-button:hover .arrow {
      transform: translateX(8px);
    }
    
    .hero-cta-button span {
      position: relative;
      z-index: 1;
    }
    
    /* Floating sparkles */
    .sparkle-effect {
      position: absolute;
      width: 8px;
      height: 8px;
      background: rgba(255, 255, 255, 0.8);
      border-radius: 50%;
      animation: sparkleFloat 3s ease-in-out infinite;
      pointer-events: none;
    }
    
    .sparkle-1 { top: 15%; left: 10%; animation-delay: 0s; }
    .sparkle-2 { top: 25%; right: 15%; animation-delay: 1s; }
    .sparkle-3 { bottom: 20%; left: 20%; animation-delay: 2s; }
    .sparkle-4 { bottom: 30%; right: 10%; animation-delay: 1.5s; }
    
    @keyframes sparkleFloat {
      0%, 100% {
        transform: translateY(0) scale(1);
        opacity: 0.4;
      }
      50% {
        transform: translateY(-20px) scale(1.5);
        opacity: 1;
      }
    }
    
    /* Responsive Design */
    @media (max-width: 768px) {
      .custom-design-hero-section {
        padding: 40px 20px;
        min-height: auto;
        overflow-x: hidden;
      }
      
      .hero-content {
        padding: 40px 25px;
        border-radius: 24px;
        max-width: 100%;
        box-sizing: border-box;
      }
      
      .hero-title {
        font-size: 2.5rem;
        margin-bottom: 16px;
        word-wrap: break-word;
        overflow-wrap: break-word;
        hyphens: auto;
      }
      
      .hero-subtitle {
        font-size: 1.1rem;
        margin-bottom: 12px;
      }
      
      .hero-description {
        font-size: 1rem;
        margin-bottom: 28px;
        line-height: 1.6;
      }
      
      .hero-features {
        gap: 10px;
        margin: 28px 0 36px 0;
      }
      
      .feature-pill {
        font-size: 0.85rem;
        padding: 10px 18px;
        white-space: nowrap;
      }
      
      .hero-cta-button {
        width: 100%;
        max-width: 100%;
        justify-content: center;
        padding: 18px 40px;
        font-size: 1rem;
        letter-spacing: 1px;
        box-sizing: border-box;
      }
      
      .hero-blob {
        display: none;
      }
      
      .sparkle-effect {
        display: none;
      }
    }
    
    @media (max-width: 480px) {
      .custom-design-hero-section {
        padding: 30px 15px;
      }
      
      .hero-content {
        padding: 35px 20px;
        border-radius: 20px;
      }
      
      .hero-title {
        font-size: 2rem;
        line-height: 1.2;
      }
      
      .hero-subtitle {
        font-size: 1rem;
      }
      
      .hero-description {
        font-size: 0.95rem;
        margin-bottom: 24px;
      }
      
      .hero-features {
        gap: 8px;
        margin: 24px 0 32px 0;
      }
      
      .feature-pill {
        font-size: 0.8rem;
        padding: 8px 16px;
      }
      
      .hero-cta-button {
        padding: 16px 32px;
        font-size: 0.95rem;
      }
    }
  </style>
  
  <!-- Floating blob backgrounds -->
  <div class="hero-blob hero-blob-1"></div>
  <div class="hero-blob hero-blob-2"></div>
  <div class="hero-blob hero-blob-3"></div>
  
  <!-- Floating sparkles -->
  <div class="sparkle-effect sparkle-1"></div>
  <div class="sparkle-effect sparkle-2"></div>
  <div class="sparkle-effect sparkle-3"></div>
  <div class="sparkle-effect sparkle-4"></div>
  
  <!-- Main content card -->
  <div class="hero-content" role="main" aria-label="Design Studio Welcome">
    <h1 class="hero-title">{{ section.settings.title | default: "🎨 Design Your Dream T-Shirt" }}</h1>
    <p class="hero-subtitle">{{ section.settings.subtitle | default: "Professional Custom Design Studio" }}</p>
    <p class="hero-description">
      {{ section.settings.description | default: "Unleash your creativity with our advanced design studio. Upload your artwork, customize colors, adjust positioning, and bring your vision to life with professional-quality custom t-shirts." }}
    </p>
    
    <!-- Premium Feature Pills with Icons -->
    <div class="hero-features" role="list">
      <div class="feature-pill" role="listitem">
        <span class="icon">⚡</span>
        <span>Real-Time Preview</span>
      </div>
      <div class="feature-pill" role="listitem">
        <span class="icon">🎨</span>
        <span>Multiple Colors</span>
      </div>
      <div class="feature-pill" role="listitem">
        <span class="icon">📏</span>
        <span>Perfect Positioning</span>
      </div>
      <div class="feature-pill" role="listitem">
        <span class="icon">💎</span>
        <span>Premium Quality</span>
      </div>
      <div class="feature-pill" role="listitem">
        <span class="icon">📥</span>
        <span>Instant Download</span>
      </div>
    </div>
    
    <!-- Premium 3D CTA Button -->
    <div style="display: flex; justify-content: center; width: 100%;">
      <button class="hero-cta-button" onclick="openCustomDesignStudio()" aria-label="Start designing your custom t-shirt">
        <span>Start Designing</span>
        <span class="arrow">→</span>
      </button>
    </div>
  </div>
</div>

<script>
// Ensure the openCustomDesignStudio function is available globally
window.openCustomDesignStudio = window.openCustomDesignStudio || function() {
  console.log('🎨 Opening Custom Design Studio...');
  
  // Hide main content
  const mainContent = document.querySelector('.main-content');
  if (mainContent) {
    mainContent.style.display = 'none';
  }
  
  // Show the custom studio
  const studio = document.querySelector('[id*="custom-tshirt-studio"]');
  if (studio) {
    studio.style.display = 'block';
    studio.scrollIntoView({ behavior: 'smooth' });
  }
  
  // Scroll to top
  window.scrollTo({ top: 0, behavior: 'smooth' });
};
</script>

{% schema %}
{
  "name": "Custom Design Hero",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "🎨 Design Your Dream T-Shirt"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Professional Custom Design Studio"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Unleash your creativity with our advanced design studio. Upload your artwork, customize colors, adjust positioning, and bring your vision to life with professional-quality custom t-shirts."
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 80
    }
  ],
  "presets": [
    {
      "name": "Custom Design Hero"
    }
  ]
}
{% endschema %}
