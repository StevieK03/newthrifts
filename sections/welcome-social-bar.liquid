{% comment %}
Welcome / Social Bar — Premium Redesign 2024
- Modern glassmorphism design with enhanced animations
- Multi-platform social integration with improved UX
- Advanced responsive design with touch-friendly interactions
- Premium visual effects and micro-interactions
{% endcomment %}

<section id="welcome-{{ section.id }}" class="welcome-wrap" role="region" aria-label="Welcome to New Thrifts">
  <div class="welcome-container">
    <div class="welcome-content">
      <div class="welcome-text">
        <div class="welcome-badge">
          <span class="badge-icon">✨</span>
          <span class="badge-text">Premium Fashion</span>
        </div>
        
        <h1 class="welcome-headline">
          {{ section.settings.heading | default: "WELCOME TO NEW THRIFTS — FRESH DROPS, GREAT PRICES." }}
        </h1>

        <div class="welcome-copy">
          {{ section.settings.body | default: "We keep your wardrobe fresh with <strong>new products weekly</strong>. From classic tees & hoodies to <strong>must-have hats, shorts, and gadgets</strong>—plus fun seasonal collections for everyone. Dive into the full catalog and find your next favorite fit today!" }}
        </div>
        
        <div class="welcome-stats">
          <div class="stat-item" data-aos="fade-up" data-aos-delay="100">
            <div class="stat-icon">👥</div>
            <span class="stat-number">10K+</span>
            <span class="stat-label">Happy Customers</span>
          </div>
          <div class="stat-item" data-aos="fade-up" data-aos-delay="200">
            <div class="stat-icon">🛍️</div>
            <span class="stat-number">500+</span>
            <span class="stat-label">Premium Products</span>
          </div>
          <div class="stat-item" data-aos="fade-up" data-aos-delay="300">
            <div class="stat-icon">⚡</div>
            <span class="stat-number">24/7</span>
            <span class="stat-label">VIP Support</span>
          </div>
        </div>

        <div class="social-section">
          <h3 class="social-title">Connect With Us</h3>
          <div class="welcome-buttons">
            {% if section.settings.facebook_url != blank %}
              <a class="social-btn facebook-btn" href="{{ section.settings.facebook_url }}" target="_blank" rel="noopener noreferrer" data-aos="zoom-in" data-aos-delay="100">
                <div class="btn-icon">
                  <svg aria-hidden="true" viewBox="0 0 24 24" width="20" height="20"><path fill="currentColor" d="M13.5 9H15V6h-1.5C11.57 6 10 7.57 10 9.5V11H8v3h2v7h3v-7h2.1l.4-3H13v-1.3c0-.45.36-.7.8-.7Z"/></svg>
                </div>
                <div class="btn-content">
                  <span class="btn-label">Follow us on</span>
                  <span class="btn-platform">Facebook</span>
                </div>
                <div class="btn-arrow">→</div>
              </a>
            {% endif %}

            {% if section.settings.instagram_url != blank %}
              <a class="social-btn instagram-btn" href="{{ section.settings.instagram_url }}" target="_blank" rel="noopener noreferrer" data-aos="zoom-in" data-aos-delay="200">
                <div class="btn-icon">
                  <svg aria-hidden="true" viewBox="0 0 24 24" width="20" height="20"><path fill="currentColor" d="M7 2h10a5 5 0 0 1 5 5v10a5 5 0 0 1-5 5H7a5 5 0 0 1-5-5V7a5 5 0 0 1 5-5Zm10 2H7a3 3 0 0 0-3 3v10a3 3 0 0 0 3 3h10a3 3 0 0 0 3-3V7a3 3 0 0 0-3-3Zm-5 4.5A5.5 5.5 0 1 1 6.5 14 5.5 5.5 0 0 1 12 8.5Zm0 2A3.5 3.5 0 1 0 15.5 14 3.5 3.5 0 0 0 12 10.5Zm5.75-3.25a1.25 1.25 0 1 1-1.25 1.25 1.25 1.25 0 0 1 1.25-1.25Z"/></svg>
                </div>
                <div class="btn-content">
                  <span class="btn-label">Follow us on</span>
                  <span class="btn-platform">Instagram</span>
                </div>
                <div class="btn-arrow">→</div>
              </a>
            {% endif %}

            {% if section.settings.twitter_url != blank %}
              <a class="social-btn twitter-btn" href="{{ section.settings.twitter_url }}" target="_blank" rel="noopener noreferrer" data-aos="zoom-in" data-aos-delay="300">
                <div class="btn-icon">
                  <svg aria-hidden="true" viewBox="0 0 24 24" width="20" height="20"><path fill="currentColor" d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
                </div>
                <div class="btn-content">
                  <span class="btn-label">Follow us on</span>
                  <span class="btn-platform">Twitter</span>
                </div>
                <div class="btn-arrow">→</div>
              </a>
            {% endif %}

            {% if section.settings.tiktok_url != blank %}
              <a class="social-btn tiktok-btn" href="{{ section.settings.tiktok_url }}" target="_blank" rel="noopener noreferrer" data-aos="zoom-in" data-aos-delay="400">
                <div class="btn-icon">
                  <svg aria-hidden="true" viewBox="0 0 24 24" width="20" height="20"><path fill="currentColor" d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-5.2 1.74 2.89 2.89 0 0 1 2.31-4.64 2.93 2.93 0 0 1 .88.13V9.4a6.84 6.84 0 0 0-1-.05A6.33 6.33 0 0 0 5 20.1a6.34 6.34 0 0 0 10.86-4.43v-7a8.16 8.16 0 0 0 4.77 1.52v-3.4a4.85 4.85 0 0 1-1-.1z"/></svg>
                </div>
                <div class="btn-content">
                  <span class="btn-label">Follow us on</span>
                  <span class="btn-platform">TikTok</span>
                </div>
                <div class="btn-arrow">→</div>
              </a>
            {% endif %}

            <!-- Amazon Affiliate Link for Women's T-Shirts -->
            <a class="social-btn affiliate-btn" href="https://amzn.to/4gVeXQ7" target="_blank" rel="noopener noreferrer sponsored" data-aos="zoom-in" data-aos-delay="500">
              <div class="btn-icon">
                <svg aria-hidden="true" viewBox="0 0 24 24" width="20" height="20"><path fill="currentColor" d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2m0-2a2 2 0 0 0-2 2v2a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2H8Z"/></svg>
              </div>
              <div class="btn-content">
                <span class="btn-label">Shop</span>
                <span class="btn-platform">Amazon T-Shirts</span>
              </div>
              <div class="btn-arrow">→</div>
            </a>
          </div>
        </div>
      </div>
      
      <div class="welcome-visual">
        <div class="visual-elements">
          <div class="floating-shape shape-1"></div>
          <div class="floating-shape shape-2"></div>
          <div class="floating-shape shape-3"></div>
          <div class="floating-dot dot-1"></div>
          <div class="floating-dot dot-2"></div>
          <div class="floating-dot dot-3"></div>
        </div>
        <div class="feature-highlights">
          <div class="highlight-item">
            <div class="highlight-icon">🛍️</div>
            <span>New Products Weekly</span>
          </div>
          <div class="highlight-item">
            <div class="highlight-icon">👕</div>
            <span>Classic Tees & Hoodies</span>
          </div>
          <div class="highlight-item">
            <div class="highlight-icon">🎃</div>
            <span>Seasonal Collections</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <style>
    /* ---- Premium Welcome Section with Modern Glassmorphism ---- */
    #welcome-{{ section.id }} {
      padding: {{ section.settings.pad_y | default: 24 }}px 16px;
      background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 25%, #16213e 50%, #0f3460 75%, #533483 100%);
      position: relative;
      overflow: hidden;
      min-height: 100vh;
    }

    /* Deep sea overlay for depth effect */
    #welcome-{{ section.id }}::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: radial-gradient(ellipse at center, transparent 0%, rgba(0, 50, 100, 0.3) 50%, rgba(0, 0, 0, 0.8) 100%);
      pointer-events: none;
      z-index: 1;
    }

    /* Floating bubble particles */
    #welcome-{{ section.id }}::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-image: 
        radial-gradient(2px 2px at 20px 30px, rgba(255, 255, 255, 0.1), transparent),
        radial-gradient(2px 2px at 40px 70px, rgba(255, 255, 255, 0.08), transparent),
        radial-gradient(1px 1px at 90px 40px, rgba(255, 255, 255, 0.06), transparent),
        radial-gradient(1px 1px at 130px 80px, rgba(255, 255, 255, 0.04), transparent),
        radial-gradient(2px 2px at 160px 30px, rgba(255, 255, 255, 0.1), transparent);
      background-repeat: repeat;
      background-size: 200px 100px;
      animation: float-bubbles 20s linear infinite;
      pointer-events: none;
      z-index: 0;
    }

    @keyframes float-bubbles {
      0% { transform: translateY(0px) translateX(0px); }
      25% { transform: translateY(-50px) translateX(10px); }
      50% { transform: translateY(-100px) translateX(-5px); }
      75% { transform: translateY(-150px) translateX(15px); }
      100% { transform: translateY(-200px) translateX(0px); }
    }

    #welcome-{{ section.id }} .welcome-container {
      max-width: 1200px;
      margin: 0 auto;
      position: relative;
      z-index: 2;
    }

    #welcome-{{ section.id }} .welcome-content {
      display: flex;
      flex-direction: column;
      gap: 32px;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }

    @media (min-width: 768px) {
      #welcome-{{ section.id }} .welcome-content {
        flex-direction: row;
        gap: 48px;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
      }
    }

    /* Welcome Badge */
    #welcome-{{ section.id }} .welcome-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 8px 16px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.1) 0%, rgba(255, 255, 255, 0.05) 100%);
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 50px;
      margin-bottom: 20px;
      backdrop-filter: blur(10px);
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      animation: badge-glow 3s ease-in-out infinite;
    }

    #welcome-{{ section.id }} .badge-icon {
      font-size: 16px;
      animation: sparkle 2s ease-in-out infinite;
    }

    #welcome-{{ section.id }} .badge-text {
      font-size: 14px;
      font-weight: 600;
      color: #ffffff;
      text-shadow: 0 0 10px rgba(255, 255, 255, 0.3);
    }

    @keyframes badge-glow {
      0%, 100% { box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1), 0 0 20px rgba(255, 255, 255, 0.1); }
      50% { box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1), 0 0 30px rgba(255, 255, 255, 0.2); }
    }

    @keyframes sparkle {
      0%, 100% { transform: scale(1) rotate(0deg); }
      50% { transform: scale(1.1) rotate(180deg); }
    }

    #welcome-{{ section.id }} .welcome-text {
      background: rgba(255, 255, 255, 0.05);
      padding: 40px 32px;
      border-radius: 24px;
      box-shadow: 
        0 20px 40px rgba(0, 0, 0, 0.3),
        0 0 0 1px rgba(255, 255, 255, 0.1),
        inset 0 1px 0 rgba(255, 255, 255, 0.2);
      border: 1px solid rgba(255, 255, 255, 0.1);
      position: relative;
      z-index: 3;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      width: 100%;
      max-width: 600px;
      backdrop-filter: blur(20px);
      transition: all 0.3s ease;
    }

    #welcome-{{ section.id }} .welcome-text:hover {
      transform: translateY(-5px);
      box-shadow: 
        0 30px 60px rgba(0, 0, 0, 0.4),
        0 0 0 1px rgba(255, 255, 255, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.3);
    }

    @media (min-width: 768px) {
      #welcome-{{ section.id }} .welcome-text {
        padding: 40px 32px;
        max-width: 600px;
        flex: 1;
      }
    }

    #welcome-{{ section.id }} .welcome-headline {
      font-family: 'Bebas Neue','Anton',system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;
      font-size: clamp(24px,4vw,36px);
      line-height: 1.1;
      margin: 0 0 16px 0;
      letter-spacing: .5px;
      color: #ffffff;
      text-wrap: balance;
      text-shadow: 0 0 20px rgba(0, 150, 255, 0.5), 0 0 40px rgba(0, 100, 200, 0.3);
    }

    #welcome-{{ section.id }} .welcome-copy {
      font-family: Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;
      font-size: 16px;
      line-height: 1.6;
      color: #b3d9ff;
      margin-bottom: 24px;
    }

    #welcome-{{ section.id }} .welcome-copy strong {
      color: #ffffff;
      font-weight: 700;
      text-shadow: 0 0 10px rgba(0, 150, 255, 0.4);
    }
    
    /* Premium Stats Section */
    #welcome-{{ section.id }} .welcome-stats {
      display: flex;
      gap: 24px;
      margin: 32px 0;
      justify-content: center;
      flex-wrap: wrap;
    }
    
    #welcome-{{ section.id }} .stat-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px 16px;
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.1) 0%, rgba(255, 255, 255, 0.05) 100%);
      border-radius: 16px;
      color: white;
      min-width: 120px;
      box-shadow: 
        0 8px 25px rgba(0, 0, 0, 0.2),
        0 0 0 1px rgba(255, 255, 255, 0.1),
        inset 0 1px 0 rgba(255, 255, 255, 0.2);
      border: 1px solid rgba(255, 255, 255, 0.1);
      transition: all 0.3s ease;
      backdrop-filter: blur(10px);
      position: relative;
      overflow: hidden;
    }

    #welcome-{{ section.id }} .stat-item::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
      transition: left 0.6s ease;
    }
    
    #welcome-{{ section.id }} .stat-item:hover {
      transform: translateY(-5px) scale(1.02);
      box-shadow: 
        0 15px 35px rgba(0, 0, 0, 0.3),
        0 0 0 1px rgba(255, 255, 255, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.3);
      border-color: rgba(255, 255, 255, 0.3);
    }

    #welcome-{{ section.id }} .stat-item:hover::before {
      left: 100%;
    }

    #welcome-{{ section.id }} .stat-icon {
      font-size: 24px;
      margin-bottom: 8px;
      filter: drop-shadow(0 0 10px rgba(255, 255, 255, 0.3));
    }
    
    #welcome-{{ section.id }} .stat-number {
      font-size: 1.8rem;
      font-weight: 800;
      line-height: 1;
      margin-bottom: 4px;
      background: linear-gradient(135deg, #ffffff 0%, #e0e0e0 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-shadow: 0 0 20px rgba(255, 255, 255, 0.3);
    }
    
    #welcome-{{ section.id }} .stat-label {
      font-size: 0.85rem;
      opacity: 0.9;
      text-align: center;
      line-height: 1.2;
      font-weight: 500;
    }

    /* Social Section */
    #welcome-{{ section.id }} .social-section {
      margin-top: 32px;
      width: 100%;
    }

    #welcome-{{ section.id }} .social-title {
      font-size: 18px;
      font-weight: 700;
      color: #ffffff;
      margin-bottom: 20px;
      text-align: center;
      text-shadow: 0 0 20px rgba(255, 255, 255, 0.3);
    }

    #welcome-{{ section.id }} .welcome-buttons {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
      justify-content: center;
    }

    /* Modern Social Buttons */
    #welcome-{{ section.id }} .social-btn {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 16px 20px;
      border-radius: 16px;
      background: rgba(255, 255, 255, 0.05);
      color: #ffffff;
      border: 1px solid rgba(255, 255, 255, 0.1);
      text-decoration: none;
      font-weight: 600;
      font-size: 14px;
      box-shadow: 
        0 8px 25px rgba(0, 0, 0, 0.2),
        0 0 0 1px rgba(255, 255, 255, 0.1),
        inset 0 1px 0 rgba(255, 255, 255, 0.2);
      transition: all 0.3s ease;
      backdrop-filter: blur(10px);
      position: relative;
      overflow: hidden;
      min-width: 200px;
    }

    #welcome-{{ section.id }} .social-btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
      transition: left 0.6s ease;
    }

    #welcome-{{ section.id }} .social-btn:hover {
      transform: translateY(-3px) scale(1.02);
      box-shadow: 
        0 15px 35px rgba(0, 0, 0, 0.3),
        0 0 0 1px rgba(255, 255, 255, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.3);
      border-color: rgba(255, 255, 255, 0.3);
    }

    #welcome-{{ section.id }} .social-btn:hover::before {
      left: 100%;
    }

    #welcome-{{ section.id }} .btn-icon {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 40px;
      height: 40px;
      border-radius: 12px;
      background: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
      transition: all 0.3s ease;
    }

    #welcome-{{ section.id }} .social-btn:hover .btn-icon {
      background: rgba(255, 255, 255, 0.2);
      transform: scale(1.1);
    }

    #welcome-{{ section.id }} .btn-content {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      flex: 1;
    }

    #welcome-{{ section.id }} .btn-label {
      font-size: 12px;
      opacity: 0.8;
      margin-bottom: 2px;
    }

    #welcome-{{ section.id }} .btn-platform {
      font-size: 16px;
      font-weight: 700;
    }

    #welcome-{{ section.id }} .btn-arrow {
      font-size: 18px;
      opacity: 0.6;
      transition: all 0.3s ease;
    }

    #welcome-{{ section.id }} .social-btn:hover .btn-arrow {
      opacity: 1;
      transform: translateX(3px);
    }

    /* Platform-specific button styles */
    #welcome-{{ section.id }} .facebook-btn:hover {
      background: linear-gradient(135deg, rgba(24, 119, 242, 0.2) 0%, rgba(24, 119, 242, 0.1) 100%);
      border-color: rgba(24, 119, 242, 0.3);
    }

    #welcome-{{ section.id }} .instagram-btn:hover {
      background: linear-gradient(135deg, rgba(225, 48, 108, 0.2) 0%, rgba(225, 48, 108, 0.1) 100%);
      border-color: rgba(225, 48, 108, 0.3);
    }

    #welcome-{{ section.id }} .twitter-btn:hover {
      background: linear-gradient(135deg, rgba(29, 161, 242, 0.2) 0%, rgba(29, 161, 242, 0.1) 100%);
      border-color: rgba(29, 161, 242, 0.3);
    }

    #welcome-{{ section.id }} .tiktok-btn:hover {
      background: linear-gradient(135deg, rgba(255, 0, 80, 0.2) 0%, rgba(255, 0, 80, 0.1) 100%);
      border-color: rgba(255, 0, 80, 0.3);
    }

    /* Special styling for Amazon affiliate button */
    #welcome-{{ section.id }} .affiliate-btn {
      background: linear-gradient(135deg, #ff9500 0%, #ff6b00 100%);
      border-color: rgba(255, 149, 0, 0.3);
      box-shadow: 
        0 8px 25px rgba(255, 149, 0, 0.3),
        0 0 0 1px rgba(255, 149, 0, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.2);
      position: relative;
      overflow: hidden;
    }

    #welcome-{{ section.id }} .affiliate-btn:hover {
      background: linear-gradient(135deg, #ff6b00 0%, #ff9500 100%);
      transform: translateY(-3px) scale(1.02);
      box-shadow: 
        0 15px 35px rgba(255, 149, 0, 0.4),
        0 0 0 1px rgba(255, 149, 0, 0.4),
        inset 0 1px 0 rgba(255, 255, 255, 0.3);
      border-color: rgba(255, 149, 0, 0.6);
    }

    /* Mobile Responsiveness */
    @media (max-width: 768px) {
      #welcome-{{ section.id }} .welcome-buttons {
        flex-direction: column;
        align-items: center;
        gap: 12px;
      }

      #welcome-{{ section.id }} .social-btn {
        min-width: 280px;
        padding: 14px 18px;
      }

      #welcome-{{ section.id }} .welcome-stats {
        gap: 16px;
        margin: 24px 0;
      }

      #welcome-{{ section.id }} .stat-item {
        min-width: 100px;
        padding: 16px 12px;
      }

      #welcome-{{ section.id }} .stat-number {
        font-size: 1.5rem;
      }

      #welcome-{{ section.id }} .social-title {
        font-size: 16px;
        margin-bottom: 16px;
      }
    }

    @media (max-width: 480px) {
      #welcome-{{ section.id }} .social-btn {
        min-width: 100%;
        padding: 12px 16px;
      }

      #welcome-{{ section.id }} .btn-icon {
        width: 36px;
        height: 36px;
      }

      #welcome-{{ section.id }} .btn-platform {
        font-size: 14px;
      }

      #welcome-{{ section.id }} .welcome-stats {
        flex-direction: column;
        align-items: center;
        gap: 12px;
      }

      #welcome-{{ section.id }} .stat-item {
        width: 100%;
        max-width: 200px;
      }
    }

    #welcome-{{ section.id }} .welcome-visual {
      position: relative;
      min-height: 200px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    @media (min-width: 768px) {
      #welcome-{{ section.id }} .welcome-visual {
        min-height: 300px;
        justify-content: center;
        align-items: center;
      }
    }

    #welcome-{{ section.id }} .visual-elements {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      pointer-events: none;
    }

    #welcome-{{ section.id }} .floating-shape {
      position: absolute;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(0, 150, 255, 0.3) 0%, rgba(0, 100, 200, 0.1) 50%, transparent 100%);
      box-shadow: 0 0 20px rgba(0, 150, 255, 0.2);
      animation: float-shape 8s ease-in-out infinite;
    }

    #welcome-{{ section.id }} .shape-1 {
      width: 60px;
      height: 60px;
      top: 20%;
      left: 10%;
      animation-delay: 0s;
    }

    #welcome-{{ section.id }} .shape-2 {
      width: 40px;
      height: 40px;
      top: 60%;
      right: 20%;
      animation-delay: 2s;
    }

    #welcome-{{ section.id }} .shape-3 {
      width: 80px;
      height: 80px;
      bottom: 20%;
      left: 20%;
      animation-delay: 4s;
    }

    #welcome-{{ section.id }} .floating-dot {
      position: absolute;
      width: 8px;
      height: 8px;
      background: radial-gradient(circle, rgba(0, 200, 255, 0.8) 0%, rgba(0, 150, 255, 0.4) 100%);
      border-radius: 50%;
      animation: float-dot 6s ease-in-out infinite;
      box-shadow: 0 0 10px rgba(0, 200, 255, 0.6);
    }

    #welcome-{{ section.id }} .dot-1 {
      top: 30%;
      right: 30%;
      animation-delay: 1s;
    }

    #welcome-{{ section.id }} .dot-2 {
      top: 70%;
      left: 30%;
      animation-delay: 3s;
    }

    #welcome-{{ section.id }} .dot-3 {
      top: 50%;
      right: 10%;
      animation-delay: 5s;
    }

    #welcome-{{ section.id }} .feature-highlights {
      background: rgba(0, 20, 40, 0.9);
      padding: 24px;
      border-radius: 16px;
      box-shadow: 0 8px 32px rgba(0, 100, 200, 0.3), 0 0 50px rgba(0, 150, 255, 0.2);
      border: 1px solid rgba(0, 150, 255, 0.3);
      position: relative;
      z-index: 3;
      width: 100%;
      max-width: 500px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      backdrop-filter: blur(10px);
    }

    @media (min-width: 768px) {
      #welcome-{{ section.id }} .feature-highlights {
        max-width: 600px;
        flex: 1;
      }
    }

    #welcome-{{ section.id }} .highlight-item {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px 0;
      border-bottom: 1px solid rgba(0, 150, 255, 0.2);
    }

    #welcome-{{ section.id }} .highlight-item:last-child {
      border-bottom: none;
    }

    #welcome-{{ section.id }} .highlight-icon {
      font-size: 20px;
      width: 32px;
      height: 32px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(0, 150, 255, 0.2);
      border-radius: 8px;
      border: 1px solid rgba(0, 150, 255, 0.3);
      box-shadow: 0 0 15px rgba(0, 150, 255, 0.3);
    }

    #welcome-{{ section.id }} .highlight-item span {
      font-weight: 600;
      color: #ffffff;
      font-size: 14px;
      text-shadow: 0 0 10px rgba(0, 150, 255, 0.4);
    }

    @keyframes float-shape {
      0%, 100% { 
        transform: translateY(0px) rotate(0deg) scale(1); 
        opacity: 0.6;
      }
      25% { 
        transform: translateY(-10px) rotate(90deg) scale(1.1); 
        opacity: 0.8;
      }
      50% { 
        transform: translateY(-20px) rotate(180deg) scale(0.9); 
        opacity: 1;
      }
      75% { 
        transform: translateY(-15px) rotate(270deg) scale(1.05); 
        opacity: 0.7;
      }
    }

    @keyframes float-dot {
      0%, 100% { 
        transform: translateY(0px) translateX(0px); 
        opacity: 0.4; 
        box-shadow: 0 0 5px rgba(0, 200, 255, 0.4);
      }
      25% { 
        transform: translateY(-8px) translateX(3px); 
        opacity: 0.7; 
        box-shadow: 0 0 15px rgba(0, 200, 255, 0.6);
      }
      50% { 
        transform: translateY(-15px) translateX(-2px); 
        opacity: 1; 
        box-shadow: 0 0 20px rgba(0, 200, 255, 0.8);
      }
      75% { 
        transform: translateY(-10px) translateX(5px); 
        opacity: 0.6; 
        box-shadow: 0 0 10px rgba(0, 200, 255, 0.5);
      }
    }

    @media (prefers-reduced-motion: reduce) {
      #welcome-{{ section.id }} .floating-shape,
      #welcome-{{ section.id }} .floating-dot {
        animation: none;
      }
    }

    @media (max-width: 767px) {
      #welcome-{{ section.id }} .welcome-visual {
        min-height: 150px;
        margin-top: 24px;
      }
      
      #welcome-{{ section.id }} .feature-highlights {
        padding: 20px;
      }
    }
  </style>

  {% schema %}
  {
    "name": "Welcome / Social Bar",
    "settings": [
      { "type": "text", "id": "heading", "label": "Headline" },
      { "type": "richtext", "id": "body", "label": "Body" },
      { "type": "url", "id": "facebook_url", "label": "Facebook URL" },
      { "type": "url", "id": "instagram_url", "label": "Instagram URL" },
      { "type": "url", "id": "twitter_url", "label": "Twitter URL" },
      { "type": "url", "id": "tiktok_url", "label": "TikTok URL" },
      { "type": "range", "id": "pad_y", "label": "Top/Bottom padding (px)", "min": 0, "max": 120, "step": 4, "default": 24 },
      { "type": "color", "id": "section_bg", "label": "Section background", "default": "#f8fafc" }
    ],
    "presets": [
      { "name": "Welcome / Social Bar" }
    ]
  }
  {% endschema %}
</section>
